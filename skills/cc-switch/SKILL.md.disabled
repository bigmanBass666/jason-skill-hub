---
name: cc-switch
description: "管理和操作 cc-switch 配置数据库的完整工具。覆盖 MCP 服务器管理、Provider（AI 模型供应商）管理、故障转移队列、代理配置等所有功能。触发场景包括：cc-switch、mcp 管理、mcp 配置、同步 mcp、添加/删除 mcp 服务器、切换 mcp 启用状态、查看 mcp 列表、添加 provider、管理 AI 供应商、切换当前供应商、故障转移配置、查看 providers 列表、cc-switch 数据库操作。即使用户只是说「帮我看看 cc-switch 配置」「加个 AI 供应商」「切一下 provider」也应触发。"
---

# cc-switch 配置管理

cc-switch 是一个 AI 客户端代理切换工具，通过 SQLite 数据库集中管理多个 AI 客户端的 MCP 服务器、Provider（模型供应商）和本地代理配置。

**项目源码**: `farion1231/cc-switch`（GitHub）

## 关键路径

| 资源 | 路径 |
|------|------|
| 数据库 | `$env:USERPROFILE\.cc-switch\cc-switch.db` |
| 设置文件 | `$env:USERPROFILE\.cc-switch\settings.json` |
| 日志 | `$env:USERPROFILE\.cc-switch\logs\cc-switch.log` |
| 备份目录 | `$env:USERPROFILE\.cc-switch\backups\` |

**重要**：所有路径中的 `C:\Users\86150` 应替换为 `$env:USERPROFILE` 以适应不同用户环境。

## 数据库操作规范

所有操作通过 `sqlite3` CLI 执行。查询时始终使用 `-header -column` 格式化输出。

**修改数据库前必须备份**：
```powershell
$ts = Get-Date -Format "yyyyMMdd_HHmmss"
$dbPath = "$env:USERPROFILE\.cc-switch\cc-switch.db"
Copy-Item $dbPath "$dbPath.backup_$ts"
```

**验证操作结果**：
```powershell
# 检查 affected rows
$sql = "UPDATE providers SET is_current = 1 WHERE id = 'xxx' AND app_type = 'claude';"
$result = sqlite3 $dbPath $sql
if ($LASTEXITCODE -ne 0) { Write-Error "SQL failed: $result" }
```

---

## 一、MCP 服务器管理

### 1.1 列出所有 MCP 服务器

```powershell
$dbPath = "$env:USERPROFILE\.cc-switch\cc-switch.db"
sqlite3 -header -column $dbPath "SELECT id, name, enabled_claude, enabled_codex, enabled_gemini, enabled_opencode, enabled_hermes, CASE WHEN server_config IS NOT NULL THEN 'configured' ELSE 'null' END AS config_status FROM mcp_servers ORDER BY name;"
```

### 1.2 查看详情

```powershell
sqlite3 -header -column $dbPath "SELECT * FROM mcp_servers WHERE id = '<name>' OR name = '<name>';"
```

`server_config` 是 JSON 字符串，支持两种格式：

**stdio 模式**：
```json
{"type": "stdio", "command": "npx", "args": ["-y", "@scope/package@latest"], "env": {"KEY": "value"}}
```

**SSE 模式**：
```json
{"url": "https://mcp.example.com/sse", "headers": {"Authorization": "Bearer xxx"}}
```

### 1.3 添加/更新服务器

```powershell
# 插入
sqlite3 $dbPath "INSERT OR IGNORE INTO mcp_servers (id, name, server_config, description, tags, enabled_claude, enabled_codex, enabled_gemini, enabled_opencode, enabled_hermes) VALUES ('<id>', '<name>', '<json_config>', '描述', '[]', 1, 1, 1, 1, 0);"

# 更新配置
sqlite3 $dbPath "UPDATE mcp_servers SET server_config = '<json_config>' WHERE id = '<id>';"
```

### 1.4 切换启用状态

可操作列：`enabled_claude`, `enabled_codex`, `enabled_gemini`, `enabled_opencode`, `enabled_hermes`

```powershell
# 启用/禁用单个客户端
sqlite3 $dbPath "UPDATE mcp_servers SET enabled_claude = 1 WHERE id = '<name>';"

# 启用全部
sqlite3 $dbPath "UPDATE mcp_servers SET enabled_claude=1, enabled_codex=1, enabled_gemini=1, enabled_opencode=1, enabled_hermes=1 WHERE id = '<name>';"
```

### 1.5 删除服务器

```powershell
sqlite3 $dbPath "DELETE FROM mcp_servers WHERE id = '<name>';"
```

### 1.6 同步 Trae CN 配置

来源：`$env:APPDATA\Trae CN\User\mcp.json`

格式特点：`mcpServers` 对象，支持 `${VAR}` 环境变量引用，有 `disabled` 字段。

同步流程：
1. 读取 Trae 的 `mcp.json`
2. 遍历 `mcpServers` 每个条目
3. `command` + `args` + `env` → `server_config` JSON
4. `disabled: true` → 所有 `enabled_*` = 0
5. 未 disabled 的已知安全服务器（sequential-thinking, github, context7, memory, everything-search, universal-db）→ 启用 claude/codex/gemini/opencode
6. `INSERT OR REPLACE` 写入

**注意**：Trae CN 的 `${VAR}` 引用保留原样，由运行时环境处理。

---

## 二、Provider（AI 模型供应商）管理

### 2.1 数据库表关系

```
providers (主表)
  ├── provider_endpoints (端点 URL，外键 → providers.id + app_type)
  ├── provider_health (健康状态，外键 → providers.id + app_type)
  └── model_pricing (模型定价，独立表)

主键：(id, app_type) — 同一 id 可存在于多个 app_type 下
```

### 2.2 查看所有 Providers

```powershell
sqlite3 -header -column $dbPath "SELECT id, name, app_type, category, is_current, in_failover_queue, sort_index, provider_type FROM providers ORDER BY app_type, sort_index, name;"
```

### 2.3 查看单个 Provider 详情

```powershell
sqlite3 -header -column $dbPath "SELECT id, name, app_type, settings_config, meta, is_current, in_failover_queue FROM providers WHERE id = '<id>';"
```

### 2.4 Provider 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | TEXT | 主键组成部分，通常用 UUID 或英文标识符（如 `xiaomi-mimo`） |
| `app_type` | TEXT | 主键组成部分，见下方 app_type 枚举 |
| `name` | TEXT | 显示名称（如 `Xiaomi MiMo`） |
| `settings_config` | JSON | **因 app_type 而异**，见下方各格式详解 |
| `meta` | JSON | 元数据（`ProviderMeta`），包含 `api_format`, `endpoint_auto_select`, `usage_script` 等 |
| `is_current` | BOOLEAN | **每个 app_type 下只能有一个为 1**（当前选中的 provider） |
| `in_failover_queue` | BOOLEAN | 是否加入故障转移队列（可与 is_current 同时为 1） |
| `sort_index` | INTEGER | 排序/故障转移优先级（越小越靠前） |
| `provider_type` | TEXT | 供应商类型（如 `github_copilot`） |
| `cost_multiplier` | TEXT | 成本倍数字符串（默认 `1.0`） |
| `limit_daily_usd` | TEXT | 每日消费限额 |
| `limit_monthly_usd` | TEXT | 每月消费限额 |
| `category` | TEXT | 分类（`cn_official`, `aggregator`, `omo`, `omo-slim` 等） |
| `notes` | TEXT | 备注信息 |

### 2.5 app_type 枚举与 settings_config 格式

每种 app_type 对应一个 AI 客户端，settings_config 写入目标不同：

| app_type | 目标客户端 | 配置文件 | settings_config 根结构 |
|----------|-----------|---------|---------------------|
| `claude` | Claude Code | `~/.claude/settings.json` | `{ "env": {...}, "enabledPlugins": {...}, "model": "..." }` |
| `claude-desktop` | Claude Desktop | 平台特定 | `{ "env": {...} }`（与 claude 格式相同） |
| `codex` | Codex | `~/.codex/auth.json` + `config.toml` | `{ "auth": {...}, "config": "TOML字符串" }` |
| `gemini` | Gemini CLI | `~/.gemini/settings.json` | `{ "config": {...}, "env": {...} }` |
| `opencode` | OpenCode | `~/.config/opencode/opencode.json` | `OpenCodeProviderConfig` |
| `openclaw` | OpenClaw | `~/.openclaw/openclaw.json` | `OpenClawProviderConfig` |
| `hermes` | Hermes Agent | `~/.hermes/config.yaml` | `HermesProviderSettingsConfig` |

#### app_type: `claude`（Claude Code）

写入 `~/.claude/settings.json`。使用 Anthropic 兼容协议。

```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "https://api.example.com",
    "ANTHROPIC_AUTH_TOKEN": "sk-xxx",
    "ANTHROPIC_MODEL": "claude-sonnet-4-20250514",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "claude-haiku-4-5",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "claude-sonnet-4-20250514",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "claude-opus-4-7"
  },
  "enabledPlugins": {
    "plugin-name@claude-plugins-official": true
  },
  "model": "opus"
}
```

常见 meta：
```json
{
  "apiFormat": "anthropic",
  "endpointAutoSelect": true,
  "commonConfigEnabled": true
}
```

#### app_type: `claude-desktop`

Claude Desktop 使用与 claude 相同的 `settings_config` 格式（`env.ANTHROPIC_BASE_URL` + `env.ANTHROPIC_AUTH_TOKEN`），通过 `meta` 中的 `claudeDesktopMode` 区分直连/代理模式。

```json
{
  "env": {
    "ANTHROPIC_BASE_URL": "https://api.example.com",
    "ANTHROPIC_AUTH_TOKEN": "sk-xxx",
    "ANTHROPIC_MODEL": "mimo-v2.5-pro",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "mimo-v2.5-pro",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "mimo-v2.5-pro",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "mimo-v2.5-pro"
  }
}
```

对应的 meta（存储在 `meta.claudeDesktopMode`）：
```json
{
  "claudeDesktopMode": "proxy",
  "claudeDesktopModelRoutes": {
    "claude-sonnet-4-6": {
      "model": "zai-org/GLM-5",
      "labelOverride": "zai-org/GLM-5",
      "supports1m": true
    }
  }
}
```

当 `mode: "proxy"` 时，`baseUrl` 是 cc-switch 本地代理地址；`"direct"` 时直接连接上游。

#### app_type: `codex`

写入 `~/.codex/auth.json`（auth 部分）和 `~/.codex/config.toml`（config 部分）。

```json
{
  "auth": {
    "OPENAI_API_KEY": "sk-xxx"
  },
  "config": "model_provider = \"my-provider\"\nmodel = \"gpt-4o\"\nmodel_reasoning_effort = \"high\"\ndisable_response_storage = true\n\n[model_providers.my-provider]\nname = \"My Provider\"\nbase_url = \"https://api.example.com/v1\"\nwire_api = \"responses\"\nrequires_openai_auth = true"
}
```

注意：`config` 是 **TOML 格式的字符串**（不是 JSON 对象）。

#### app_type: `gemini`

写入 `~/.gemini/settings.json`。Gemini 使用嵌套结构，包含 `config` 和 `env` 两部分。

```json
{
  "config": {
    "env": {
      "GEMINI_API_KEY": "AIzaSyxxx"
    },
    "mcpServers": {
      "fetch": {
        "command": "uvx",
        "args": ["mcp-server-fetch"],
        "timeout": 60000
      }
    },
    "model": {
      "name": "gemini-2.5-flash"
    },
    "security": {
      "auth": {
        "selectedType": "gemini-api-key"
      }
    }
  },
  "env": {
    "GEMINI_API_KEY": "AIzaSyxxx",
    "GOOGLE_GEMINI_BASE_URL": "https://generativelanguage.googleapis.com/v1beta"
  }
}
```

**验证规则**：
- 非空 `env` 必须包含 `GEMINI_API_KEY`
- 空 `env` 表示 OAuth 模式（跳过验证）

#### app_type: `opencode`

写入 `~/.config/opencode/opencode.json`。使用 AI SDK 兼容格式。

```json
{
  "npm": "@ai-sdk/openai-compatible",
  "name": "Provider Display Name",
  "options": {
    "baseURL": "https://api.example.com/v1",
    "apiKey": "sk-xxx",
    "headers": { "X-Custom": "value" }
  },
  "models": {
    "gpt-4o": {
      "name": "GPT-4o",
      "limit": { "context": 128000, "output": 16384 },
      "options": {},
      "cost": { "input": 2.5, "output": 10 }
    }
  }
}
```

#### app_type: `openclaw`

写入 `~/.openclaw/openclaw.json` 的 `models.providers.<id>` 节点。

```json
{
  "baseUrl": "https://api.example.com/v1",
  "apiKey": "sk-xxx",
  "api": "openai-completions",
  "models": [
    {
      "id": "claude-sonnet-4-6",
      "name": "Claude Sonnet 4.6",
      "contextWindow": 200000,
      "cost": { "input": 3, "output": 15 },
      "reasoning": true
    }
  ],
  "headers": { "User-Agent": "Mozilla/5.0" }
}
```

#### app_type: `hermes`

写入 `~/.hermes/config.yaml` 的 `custom_providers` 列表。

```json
{
  "name": "my-provider",
  "base_url": "https://api.example.com/v1",
  "api_key": "sk-xxx",
  "api_mode": "chat_completions",
  "models": [
    { "id": "my-model", "name": "My Model", "context_length": 1000000 }
  ]
}
```

### 2.6 添加 Provider

新增 provider 时，需要同时向 `providers` 表和 `provider_endpoints` 表写入数据。

#### 步骤 1：构造 settings_config

根据目标 app_type 选择上述对应格式。如果用户提供了参考 provider，建议先查询参考数据：
```powershell
sqlite3 $dbPath "SELECT settings_config, meta FROM providers WHERE name LIKE '%<参考名>%' AND app_type = '<app_type>';"
```

#### 步骤 2：生成 id

- 推荐使用英文短横线命名（如 `zhipu-glm`, `xiaomi-mimo`）
- 或使用 UUID（与现有数据一致）
- 注意：`(id, app_type)` 是联合主键，同一 id 可有多个 app_type 行

#### 步骤 3：INSERT

```powershell
$settings = '{"env":{"ANTHROPIC_BASE_URL":"https://api.example.com","ANTHROPIC_AUTH_TOKEN":"sk-xxx"}}'
$meta = '{"apiFormat":"anthropic","endpointAutoSelect":true}'
$id = "my-provider"
$name = "My Provider"
$appType = "claude"

$sql = "INSERT OR IGNORE INTO providers (id, app_type, name, settings_config, meta, created_at, is_current, in_failover_queue, cost_multiplier) VALUES ('$id', '$appType', '$name', '$settings', '$meta', strftime('%s','now')*1000, 0, 0, '1.0');"
sqlite3 $dbPath $sql
```

#### 步骤 4：添加端点

```powershell
sqlite3 $dbPath "INSERT INTO provider_endpoints (provider_id, app_type, url, added_at) VALUES ('$id', '$appType', '<api_url>', strftime('%s', 'now') * 1000);"
```

### 2.7 更新 Provider

```powershell
# 更新 settings_config
sqlite3 $dbPath "UPDATE providers SET settings_config = '<json>' WHERE id = '<id>' AND app_type = '<app_type>';"

# 更新 meta
sqlite3 $dbPath "UPDATE providers SET meta = '<json>' WHERE id = '<id>' AND app_type = '<app_type>';"

# 同时更新多个字段
sqlite3 $dbPath "UPDATE providers SET name = '<新名称>', sort_index = <新排序> WHERE id = '<id>' AND app_type = '<app_type>';"
```

### 2.8 删除 Provider

```powershell
# 同时删除关联的 endpoints 和 health 记录（外键 CASCADE 会自动处理）
sqlite3 $dbPath "DELETE FROM providers WHERE id = '<id>' AND app_type = '<app_type>';"

# 删除所有 app_type 下的该 provider
sqlite3 $dbPath "DELETE FROM providers WHERE id = '<id>';"
```

**注意**：删除 `is_current = 1` 的 provider 会导致该 app_type 没有活跃 provider，cc-switch 会自动选择下一个。

### 2.9 切换当前 Provider（设置 is_current）

每个 app_type 下只能有一个 `is_current = 1`。**切换时必须先清除同 app_type 下其他 provider 的 is_current**。

```powershell
$dbPath = "$env:USERPROFILE\.cc-switch\cc-switch.db"
$appType = "claude"
$newProviderId = "my-provider"

# 使用事务确保原子性
$sql = "BEGIN TRANSACTION; UPDATE providers SET is_current = 0 WHERE app_type = '$appType'; UPDATE providers SET is_current = 1 WHERE id = '$newProviderId' AND app_type = '$appType'; COMMIT;"
sqlite3 $dbPath $sql
```

### 2.10 管理故障转移队列

- `in_failover_queue = 1` → 该 provider 参与自动故障转移
- 故障转移队列按 `sort_index` 排序
- `is_current` 和 `in_failover_queue` **相互独立**，可以同时为 1

```powershell
# 加入队列
sqlite3 $dbPath "UPDATE providers SET in_failover_queue = 1 WHERE id = '<id>' AND app_type = '<app_type>';"

# 移出队列
sqlite3 $dbPath "UPDATE providers SET in_failover_queue = 0 WHERE id = '<id>' AND app_type = '<app_type>';"

# 查看队列（按优先级排序）
sqlite3 -header -column $dbPath "SELECT id, name, sort_index, is_current FROM providers WHERE app_type = '<app_type>' AND in_failover_queue = 1 ORDER BY COALESCE(sort_index, 999999), id;"
```

### 2.11 查看 Provider 端点

```powershell
sqlite3 -header -column $dbPath "SELECT pe.id, p.name, pe.url, pe.added_at FROM provider_endpoints pe JOIN providers p ON pe.provider_id = p.id AND pe.app_type = p.app_type ORDER BY p.name;"
```

---

## 三、Proxy 配置

### 3.1 查看代理配置

```powershell
sqlite3 -header -column $dbPath "SELECT * FROM proxy_config;"
```

`proxy_config` 表以 `app_type` 为主键，**仅支持 `claude`, `codex`, `gemini` 三种 app_type**。

主要字段：

| 字段 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `proxy_enabled` | INTEGER | 0 | 是否启用代理 |
| `listen_address` | TEXT | `127.0.0.1` | 监听地址 |
| `listen_port` | INTEGER | 15721 | 监听端口 |
| `auto_failover_enabled` | INTEGER | 0 | 是否启用自动故障转移 |
| `max_retries` | INTEGER | 3 | 最大重试次数 |
| `streaming_first_byte_timeout` | INTEGER | 60 | 流式首字节超时（秒） |
| `streaming_idle_timeout` | INTEGER | 120 | 流式空闲超时（秒） |
| `non_streaming_timeout` | INTEGER | 600 | 非流式超时（秒） |
| `circuit_failure_threshold` | INTEGER | 4 | 熔断失败阈值 |
| `circuit_success_threshold` | INTEGER | 2 | 熔断成功阈值 |
| `circuit_timeout_seconds` | INTEGER | 60 | 熔断超时（秒） |
| `circuit_error_rate_threshold` | REAL | 0.6 | 熔断错误率阈值 |
| `circuit_min_requests` | INTEGER | 10 | 熔断最小请求数 |
| `live_takeover_active` | INTEGER | 0 | 是否激活 live takeover |

---

## 四、其他数据库表

| 表名 | 用途 | 主要字段 |
|------|------|----------|
| `skills` | Skill 管理 | `id`, `name`, `directory`, `enabled_claude/codex/gemini/opencode/hermes` |
| `skill_repos` | Skill 仓库源 | `owner`, `name`, `branch`, `enabled` |
| `prompts` | 提示词模板 | `id`, `app_type`, `name`, `content`, `enabled` |
| `model_pricing` | 模型定价 | `model_id`, `input_cost_per_million`, `output_cost_per_million` |
| `usage_daily_rollups` | 每日用量统计 | `date`, `app_type`, `provider_id`, `model`, `request_count`, `total_cost_usd` |
| `session_log_sync` | 会话日志同步 | `file_path`, `last_modified`, `last_line_offset` |
| `stream_check_logs` | 连通性检查日志 | `provider_id`, `app_type`, `status`, `success`, `response_time_ms` |
| `proxy_live_backup` | 代理实时备份 | `app_type`, `original_config`, `backed_up_at` |
| `proxy_request_logs` | 代理请求日志 | `request_id`, `provider_id`, `model`, `input_tokens`, `output_tokens`, `total_cost_usd` |
| `provider_health` | 供应商健康状态 | `provider_id`, `app_type`, `is_healthy`, `consecutive_failures` |

---

## 五、Meta 字段结构（ProviderMeta）

`meta` 字段是 JSON 字符串，存储在 `providers.meta` 列中。主要字段：

| 字段 | 类型 | 说明 |
|------|------|------|
| `apiFormat` | STRING | `"anthropic"`, `"openai_chat"`, `"openai_responses"`, `"gemini_native"` |
| `endpointAutoSelect` | BOOLEAN | 测速后自动选择最佳端点 |
| `commonConfigEnabled` | BOOLEAN | 是否应用通用配置片段 |
| `claudeDesktopMode` | STRING | `"direct"` 或 `"proxy"`（仅 claude-desktop） |
| `claudeDesktopModelRoutes` | OBJECT | Desktop 模型路由映射（仅 claude-desktop） |
| `usageScript` | OBJECT | 用量查询脚本 |
| `providerType` | STRING | `"github_copilot"`, `"codex_oauth"` 等 |
| `isPartner` | BOOLEAN | 合作伙伴标记 |
| `costMultiplier` | STRING | 成本倍数 |
| `limitDailyUsd` | STRING | 每日消费限额 |
| `limitMonthlyUsd` | STRING | 每月消费限额 |

---

## 六、验证规则与约束

1. **proxy_config.app_type CHECK 约束**：`CHECK (app_type IN ('claude','codex','gemini'))` — 只有这三个 app 支持代理模式
2. **providers 复合主键**：`(id, app_type)` — 同一 id 可以在不同 app_type 下存在
3. **AppType 枚举**：`claude`, `claude-desktop`, `codex`, `gemini`, `opencode`, `openclaw`, `hermes`
4. **Gemini 验证**：非空 env 必须包含 `GEMINI_API_KEY`；空 env 表示 OAuth 模式（跳过验证）
5. **Claude Desktop 验证**：直连模式要求 `ANTHROPIC_BASE_URL` + `ANTHROPIC_AUTH_TOKEN`；代理模式要求模型路由映射 + base_url + api_key
6. **数据库版本**：当前 schema 版本为 v10，通过 `add_column_if_missing` 保证迁移幂等性

---

## 七、常见问题排查

### Provider 添加后不显示
1. 确认 `is_current` 和 `in_failover_queue` 字段值
2. 确认查询时 `app_type` 匹配当前选中的客户端
3. 重启 cc-switch 应用

### settings_config JSON 格式错误
先参考同 app_type 的现有 provider：
```powershell
sqlite3 $dbPath "SELECT settings_config FROM providers WHERE app_type = 'claude' LIMIT 1;"
```

### 需要跨 app_type 复制 provider
同一 `id` 可在不同 `app_type` 下有多行，每行的 `settings_config` 格式不同：
```powershell
sqlite3 $dbPath "INSERT INTO providers (id, app_type, name, settings_config, meta, created_at, is_current, in_failover_queue, cost_multiplier) SELECT id, 'claude-desktop', name, '<新settings_config>', '<新meta>', strftime('%s','now')*1000, 0, 0, cost_multiplier FROM providers WHERE id = '<id>' AND app_type = 'claude' LIMIT 1;"
```

### 删除当前活跃 Provider 失败
cc-switch 遵循"最小侵入"原则，至少保留一个活跃 provider。如需删除，先切换到另一个 provider。

### JSON 转义问题
在 PowerShell 中写入 JSON 时，内部引号需要转义。建议使用变量构造：
```powershell
$settings = @{env=@{ANTHROPIC_BASE_URL="https://api.example.com"}} | ConvertTo-Json -Compress
```
